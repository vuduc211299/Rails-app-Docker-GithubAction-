<h1>Cat Cat images</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
      <th>Image</th>
    </tr>
  </thead>

  <tbody>
    <% @images.each do |image| %>
      <tr>
        <td><%= image.title %></td>
        <td><%= image.description %></td>
        <td><img width='50px' hight='50px'  src="<%= ENV.fetch("AWS_IMG_SRC") %><%= image.url %>" alt="Image from S3"><td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= button_to 'Sign out', destroy_user_session_path, method: :delete %>

<br>
